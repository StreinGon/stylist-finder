{
  "routes": [
    {
      "method": "GET",
      "path": "/profile",
      "handler": "User.profile",
      "config": {
        "policies": [
          "isAuthenticated"
        ]
      }
    },
    {
      "method": "GET",
      "path": "/allStylist",
      "handler": "User.allStylist",
      "config": {
        "policies": [
          "isAuthenticated"
        ]
      }
    },
    {
      "method": "POST",
      "path": "/mediaprod",
      "handler": "User.mediaprod",
      "config": {
        "policies": [
          "isAuthenticated"
        ]
      }
    },
    {
      "method": "POST",
      "path": "/wedding",
      "handler": "User.wedding",
      "config": {
        "policies": [
          "isAuthenticated"
        ]
      }
    },
    {
      "method": "POST",
      "path": "/schedule",
      "handler": "User.schedule",
      "config": {
        "policies": [
          "isAuthenticated"
        ]
      }
    }
  ]
}